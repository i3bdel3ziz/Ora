# Timesheet App — V1.4 (Hotfix)
This hotfix escapes the `</script>` tag embedded in the PDF export HTML string, which could break the outer inline script on some browsers/hosts.

Everything else is the same as V1.4.
